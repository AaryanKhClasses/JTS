package test;

public class Main {
    public static void main(String[] args) { }

    function varTest(): void {
        let num1 = 20;
        const str1: string = "Hello";

        let arr1: string[] = ["a", "b", "c"];
        let arr2: number[] = [1, 2, 3];
        let arr2d: number[][] = [[1, 2], [3, 4]];
        let untypedArray = [1, 2, 3];
        const testArr: any[] = [1, "two", 3.0, true];

        const arr3: Array<string> = ["x", "y", "z"];
        let arr4: Array<number> = [4, 5, 6];
        let arr4d: Array<Array<number>> = [[7, 8], [9, 10]];
    }

    function funcTest(a: number, b: number): number[] {
        let result: number[] = [a + b, a - b, a * b, a / b];
        return result;
    }

    function loopTest(): void {
        for(const item of testArr) {
            console.log(item);
        }

        for(let i: number = 0; i < arr2.length; i++) {
            console.log(arr2[i]);
        }
    }
}
