package test;

public class Main {
    public static void main(String[] args) {
        let age = 20;
        const name = "Test Name";
        let a2: number = 10;
        const a3: string = "Hello";
        // let should not change
        /*
            const
            function
        */

        char c = '\'';
        String text = "let const function";
        if(age == 20) console.log(text);

        function test(a: number, b: number): number {
            return a + b;
        }
    }
}
